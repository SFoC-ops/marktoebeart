// Phase 2 placeholder.
export default function AboutPage() {
  return (
    <section className="mx-auto max-w-[var(--container-site)] px-5 py-24 md:px-10 md:py-40">
      <h1 className="font-display text-5xl uppercase leading-[0.95] md:text-8xl">About</h1>
      <p className="mt-6 max-w-xl text-[color:var(--color-ink-dim)]">
        Portrait, bio, Camp Beer marketing role, Pacific / Light gallery show, credentials strip.
        In build.
      </p>
    </section>
  );
}
